import { prefix } from '@/const/const-data';

export const settingApis = {
    set: `${prefix}/setting/set`,
    get: `${prefix}/setting/get`,
    listConfig: `${prefix}/setting/listConfig`,
};
export default settingApis;
