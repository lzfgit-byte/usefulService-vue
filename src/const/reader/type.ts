export default {};
export interface ResultEntity {
    code: number;
    data?: any;
    msg: string;
}
export interface BookInfo {
    id: string;
    bookName?: string;
    coverImg?: string;
    total?: number;
}
