import { prefix } from '@/const/const-data';

export const tempApis = {
    clearTemp: `${prefix}/temp/clearTemp`,
    getSaveData: `${prefix}/temp/getSaveData`,
    doSaveData: `${prefix}/temp/doSaveData`,
};
export default tempApis;
