import { prefix } from '@/const/const-data';

export const proxyApis = {
    getImgByte: `${prefix}/hWord/getImgByte`,
    getVideoByte: `${prefix}/hWord/getVideoByte`,
    getHtml: `${prefix}/hWord/getHtml`,
};
export default proxyApis;
